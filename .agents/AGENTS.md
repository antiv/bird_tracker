# Project Rules

This is a Flutter project. Please follow these guidelines:
1. **Always use FVM**: For any Flutter or Dart command execution, always use FVM (e.g. `fvm flutter ...`, `fvm dart ...`).
2. **Project Recognition**: Treat this project as a Flutter project and use appropriate Flutter tools/commands for building, dependency resolution, testing, and static analysis.
